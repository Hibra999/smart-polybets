"""agent/tools/ — wrappers agent-facing de cada área.

Aquí (no en functions/) vive todo lo que tiene estado o llama a una API: la
construcción del DjangoClient, la lectura de adapters, etc. Las functions/ siguen
siendo puras.
"""
